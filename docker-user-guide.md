# Docker ユーザーガイド

アプリを起動するために必要な手順だけをまとめたガイドです。Dockerの知識は不要です。

---

## 1. Docker Desktop をインストールする（初回のみ）

1. https://www.docker.com/products/docker-desktop/ を開く
2. お使いのOS（Windows / Mac）向けのインストーラーをダウンロード
3. インストーラーを実行してインストール完了
4. Docker Desktop を起動する（タスクバーにクジラのアイコンが表示されれば OK）

---

## 2. アプリの設定ファイルを用意する（初回のみ）

プロジェクトフォルダに `.env` というファイルを作成し、以下を記入してください。

```
OPENAI_API_KEY=あなたのAPIキーをここに貼る
```

> `.env.example` というファイルがあれば、それをコピーして `.env` にリネームしてから編集するだけです。

---

## 3. アプリを起動する

プロジェクトフォルダをターミナル（コマンドプロンプト）で開き、以下を実行します。

```bash
docker compose up
```

しばらく待つと次のようなメッセージが出ます。

```
Application startup complete.
```

ブラウザで http://localhost:8000 を開くとアプリが使えます。

---

## 4. アプリを停止する

ターミナルで `Ctrl + C` を押すと停止します。

完全に片付けたい場合は続けて次のコマンドを実行します。

```bash
docker compose down
```

---

## よくある質問

**Q. 起動が遅い**
初回は必要なファイルをダウンロードするため数分かかります。2回目以降は速くなります。

**Q. ポートが使用中というエラーが出る**
別のアプリが 8000 番ポートを使っています。Docker Desktop でコンテナを停止してから再度試してください。

**Q. アプリが動かない**
`.env` に `OPENAI_API_KEY` が正しく設定されているか確認してください。

**Q. データはどこに保存されますか？**
プロジェクトフォルダ内の `data/` フォルダに保存されます。Dockerを削除してもデータは消えません。
